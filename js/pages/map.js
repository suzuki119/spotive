/**
 * map.js
 * 観戦マップ画面（pages/map/map.php）の処理。
 * Leaflet 1.9.4 を使う。バージョンは各自で変えないこと。
 */

document.addEventListener("DOMContentLoaded", () => {
  // TODO: 地図の初期化と、試合ピンの配置
  // OpenStreetMap を使うので attribution の表記は必須
});
